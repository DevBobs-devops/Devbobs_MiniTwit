\section{Process Perspective}\label{process-perspective}

\subsection{CI/CD Pipelines}\label{cicd-pipelines}
The CI/CD pipeline consists of three GitHub Action workflows, which can be seen in
\code{/.github/workflows/}. All workflows are run on
\code{ubuntu-24.04}. The first part of our CI pipeline (Build and test workflow) runs on pull-requests to main. 
The whole pipeline is run on pushes to main. 
The following section will give a description of each workflow and how a feature goes to production. This is also illustrated in figure~\ref{cicd-diagram}.
%TODO: Which standard does this diagram follow?
%TODO split up diagram
%TODO fix: maybe building the artefact is actually considered CI?

\newgeometry{top=1cm, bottom=0cm, left=3cm, right=3cm}
\begin{figure} [H]
    \pumlfile{cicd.puml}
    \caption{UML activity diagram of CI/CD pipeline. Swim lanes indicate responsible system or actor for each action.}
    \label{cicd-diagram}
\end{figure}
\restoregeometry


\subsubsection{Build and test}\label{build-and-test}
The first workflow (``\textit{Build and Test}''), forms the continous integration of our pipeline and consists of the following steps that run in parrallel.
\begin{enumerate}
    \item \textbf{Code formatter with Csharpier}: Checks the layout of the code. 
    This helps ensure that the layout is alike throughout the codebase. It does not automatically apply layout changes, but reports it back on GitHub.
    \item \textbf{Dockerfile linter with Hadolint}: Lints the MiniTwit dockerfile.
    \item \textbf{Build the application}: Builds the applications and:
    \begin{itemize}
        \item Runs unit and integration tests.
        \item Runs tests on the API / simulator endpoint.
        \item Runs E2E / Playwright tests.
    \end{itemize}
\end{enumerate}

\subsubsection{CI/CD}
The (``\textit{CI/CD}'') workflow, forms our continous deployment and runs on a successful \textit{Build and Test} workflow and push to main. It consists of the following steps that run sequentially.
\begin{enumerate}
    \item \textbf{Dockerhub}:
    \begin{itemize}
        \item Github Actions builds and pushes the \code{MiniTwit}, \code{Grafana}, \code{Alloy}, \code{Loki} and \code{Prometheus} docker images to Dockerhub.
    \end{itemize}
    \item \textbf{SSH and Deploy}
    \begin{itemize}
        \item SSH into the leader node.
        \item Copy the stack file \path{/itu-minitwit/infrastructure/docker_swarm/stack/minitwit_stack.yml} to the node.
        \item Deploy to the swarm.
    \end{itemize}
\end{enumerate}

\subsubsection{Compile report}
As part of our report writing, github actions compiles the report and stores it as an artefact.

\subsubsection{Release}\label{release}

The ``\textit{release}'' workflow runs on a successful CI/CD run. It builds and zips
the MiniTwit application for Windows, Mac and Linux, zips the source code and this report.
In hindsight our releases are not perfect.
We automatically increased the version of each release which is why we ended up with version \texttt{0.0.259}, having no major or minor versions.
The releases did not contain any description, creating bad transparency.
Lastly, instead of zipping our Minitwit application, we could have used our docker images to encapsulate each part of the system in releases.
For now, these can be found at \url{https://hub.docker.com/u/powsdaws}.

\subsubsection{SonarQube}\label{sonarqube}
SonarQube runs on pull requests as a step in our CI pipeline. It checks for security issues, code smells and code quality.
SonarQube also gives a Quality Gate, that fails if the threshold for the pull-request is too low 
e.g. Security, Reliability and Maintainability.
SonarQube is not blocking 
meaning we could still merge, even if SonarQube fails. It's more used as a reminder of better coding practices and 
error handling.   

\subsection{Monitoring}\label{monitoring}
The system uses Prometheus and Grafana for monitoring. 
Prometheus scrapes the \code{/metrics} endpoint on each MiniTwit service instance.
Grafana then queries Prometheus, which displays and visualises the data, see figure~\ref{mon-diagram}. 
Monitoring is done on the business level, application level, and infrastructure level.

\subsubsection{Business monitoring}\label{business-monitoring}

On the business level we monitor:
\begin{itemize}
    \item Number of created accounts.
    \item Average follower count per user.
    \item Total number of cheeps over time.
\end{itemize}

\includegraphics[width=\textwidth]{images/monitoring_business.png}

\subsubsection{Application monitoring}\label{application-monitoring}

On the application level, we monitor the durations of different database queries that
MiniTwit makes:
\begin{itemize}
    \item GetCheeps - Gets cheeps for a given page.
    \item GetCheepsFromAuthor - Getting an Author's cheeps.
    \item AddCheep - Create a cheep.
\end{itemize}

\includegraphics[width=\textwidth]{./images/monitoring_queries.png}

\subsubsection{Infrastructure
monitoring}\label{infrastructure-monitoring}

On the infrastructure level, we monitor:
\begin{itemize}
    \item The status of nodes.
    \item Memory usage of nodes.
    \item CPU usage of nodes.
    \item HTTP requests per minute for each node.
\end{itemize}

\includegraphics[width=\textwidth]{./images/system_status.png}

\subsection{Logging}\label{processlogging}
The application uses a Grafana-loki-alloy stack to log all services. The logging setup can be seen in figure \ref{mon-diagram}.

\subsubsection{What we log}\label{what-we-log}
We currently log the logs from each docker service, as described in our 
\sloppy\code{/infrastructure/docker\_swarm/stack/minitwit\_stack.yml} file. As will be discussed in \ref{reflection-logging}, 
more comprehensive logging would have been beneficial.


\subsubsection{Aggregation of logs}\label{aggregation-of-logs}
We use Alloy to collect logs from each node and forward them to Loki, see figure~\ref{logging-dashboard}. The logs are displayed on grafana.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{./images/logging_dashboard.png}
\caption{Logging dashboard}
\label{logging-dashboard}
\end{figure}


\begin{figure} [H]
    \pumlfile{monitoring.puml}
    \caption{Deployment diagram of the monitoring and logging setup}
    \label{mon-diagram}
\end{figure}



\subsection{Security hardening}\label{security-hardening}
To protect against supply line attacks, we would select docker hardened images from \url{dhi.io} to use for our dependencies; Prometheus, Alloy, Loki and Grafana.
We would also go through our MiniTwit application and adjust it to comply with the rules at \url{https://cheatsheetseries.owasp.org/cheatsheets/Docker_Security_Cheat_Sheet.html}.
However, we did not manage to fully implement this, so it is not used in production. \\

The system uses a reverse proxy, which takes HTTPS requests and decrypts them.
This is done with Caddy, as was seen in figure \ref{deployment-diagram}.
Upon reflection on the system, and the information it exposes, it should have been hidden behind the reverse proxy and had firewalls, as many ports can still be reached with HTTP.

\subsection{Availability and scaling}\label{availability-and-scaling}
To scale and ensure availability of the application Terraform and Docker
Swarm is used.

\subsubsection{Availability}\label{availability}
The application consists of five nodes that are a part of a swarm
cluster, 2 workers, 2 managers and 1 leader, creating redundancy, that, together, run four replicated MiniTwit services.
All nodes run Alloy.
The ``Leader'' node also runs Caddy, Grafana, Prometheus and Loki.
Figure \ref{deployment-diagram} illustrates this.
\sloppy See
\code{terraform/minitwit\_swarm\_cluster.tf} and
\code{docker\_swarm/stack/minitwit\_stack.yml} under
\code{/infrastructure} for the code. Having multiple services running MiniTwit ensures that even if a docker service or swarm node
fails, the application is still available.

\textbf{Single Point of Failure}: Multiple services like Caddy and Grafana are pinned to the ``Leader'' node. 
This creates SPOF for those services, e.g. only having one Caddy load balancer service,
living on the ``Leader'' node, that takes HTTPS requests on our \code{devbobs.tech} domain. So, if the ``Leader'' 
fails,  HTTPS request would no longer be recieved by the system. 
If the ``Leader'' goes down, it is still possible to access the other nodes
IPs on port :8080 without HTTPS. The docker ingress routing mesh
will take care of load balancing, instead of Caddy.

\subsubsection{Scaling}\label{scaling}
The application can be scaled in two different ways: \textbf{Vertical
scaling}: A single node can be scaled vertically either by resizing the
node in DigitalOcean or changing the \code{size} field in
\code{minitwit\_swarm\_cluster.tf} and applying it with Terraform.
\textbf{Horizontal scaling}: The deployment of the application can be
scaled horizontally by increasing the amount of nodes in the cluster. This
can be done by increasing the \code{count} number of e.g.~a Worker
node and then running the \code{bootstrap.sh} script 
again, which applies the change with Terraform.
Horizontal scaling can also be achieved by increasing the amount of MiniTwit instances per node, increasing redundancy.
When scaling horizontally the load balancer distributes the requests.
